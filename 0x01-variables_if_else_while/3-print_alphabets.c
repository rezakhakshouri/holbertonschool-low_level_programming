#include <stdio.h>

/**
* main - 0 through 9
* description: gets numbers
* Return: 0
*/

int main(void)
{
	char c;
	char b;

	for (c = 'a' ; c <= 'z' ; c++)
{
	putchar(c);
}
	b == 'A';
	while (b <= 'Z')
{	putchar(b);
}
{
	putchar('\n');
}
	return (0);
}
